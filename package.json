{
  "name": "openserv-defi-agent",
  "version": "1.0.0",
  "description": "DeFi analytics agent for the OpenServ marketplace — analyzes pools, scans tokens, and finds yield opportunities on Base",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "tsx src/index.ts",
    "demo": "tsx scripts/demo.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@openserv-labs/sdk": "^2.4.1",
    "dotenv": "^16.4.7",
    "viem": "^2.23.0",
    "zod": "^3.24.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
